def f(n):
    if n <= 1:
        return n
    else:
        return (f(n-1) + f(n-2))
nterms = 12
if nterms <= 0:
    print ("enter a positive value")
else:
    print ("fib seq:")
    for j in range (nterms):
         print(f(j))
         
